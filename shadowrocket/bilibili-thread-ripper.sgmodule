#!name=Bilibili 线程撕裂者（移动端）
#!desc=给 B 站官方 App 加速：把每个视频分片请求拆成多块，向多个 CDN 节点并发下载后拼好交还播放器。不需要 HTTPS 解密。设置页：http://btr.settings/
#!author=vic233333
#!homepage=https://github.com/vic233333/Bilibili-thread-ripper-mobile
#!icon=https://raw.githubusercontent.com/MrTangLuyao/Bilibili-thread-ripper/main/icons/icon-128.png
#!category=B 站
#!system=ios

# 原作：MrTangLuyao 的 Bilibili 线程撕裂者（MIT） https://github.com/MrTangLuyao/Bilibili-thread-ripper
# 这个模块只处理明文 http 的视频分片，也就是 B 站 App 实际下载视频的请求；HTTPS 的接口请求一律不碰。

[Script]
# 所有 B 站视频 CDN 主机的明文请求都交给脚本：能整体换节点的 /upgcxcode/ 分片才会被拆分，其余（PCDN 等）只记入设置页的统计，原样放过。
btr-media = type=http-request, pattern=^http:\/\/[^\/]+\.(bilivideo\.(com|cn|net)|akamaized\.net|szbdyd\.com)(:\d+)?\/, argument=mode=http, requires-body=0, timeout=45, engine=webview, script-path=https://raw.githubusercontent.com/vic233333/Bilibili-thread-ripper-mobile/main/shadowrocket/bilibili-thread-ripper.js?role=media&v=0.1.7

# 设置页与统计：在 Safari 里打开 http://btr.settings/
btr-settings = type=http-request, pattern=^http:\/\/btr\.settings(?:\/.*)?$, argument=mode=http, requires-body=0, timeout=10, engine=webview, script-path=https://raw.githubusercontent.com/vic233333/Bilibili-thread-ripper-mobile/main/shadowrocket/bilibili-thread-ripper.js?role=settings&v=0.1.7

[Host]
# btr.settings 不是真实域名。它要解析到一个会进入 Shadowrocket 隧道的地址，请求才会在发出前
# 被上面的脚本接住（127.0.0.1 走本机回环，不经过隧道）。这里用 GitHub Pages 的地址占位：
# 万一脚本没加载，Safari 只会看到 GitHub 的 404 页。
btr.settings = 185.199.108.153
